From scratch implementation of linear regression in C. Just a project to practice my C skills.  

Currently only supports numerical datasets and considers any other entry a NaN.  

main.c has an example that shows reading a dataset, splitting it to train and test datasets, normalizing its columns, training a model on it, and calculating the model's metrics (MAE, MSE, R2, etc).  
